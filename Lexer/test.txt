#include "math.h"
#include "stdio.h"

void main(){
    int a, b;
	int d[1000];
	double v;
	v = 0.158456e+5;
    writeString("Hello World!!\n");
    if (a == square(b) && a < b && a >= (int) v){
        a++;
        b+=3;
	}
	for(i = 0; i < 1000; i++)
		for(j = 0; j < i; j++)
			d[i][j] = 1;
	return 0;
}